# TadaAsk Search Modes

Fast mode uses the raw FTS and vector results before RRF and optional reranking.

## Adaptive

Adaptive mode expands the query only when the initial raw-search confidence is insufficient.

Full mode always performs query expansion before the final fusion and reranking stages.
