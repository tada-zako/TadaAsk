# LunarCache 2.4.1 Release Notes

## Desktop support

Version 2.4.1 adds Linux as a supported desktop platform. The same release keeps the existing Windows and macOS packages.

The release also improves cache compaction after interrupted imports.
