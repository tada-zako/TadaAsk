# LunarCache Configuration

The primary configuration file is named `lunarcache.toml`.

## Network

The service listens on HTTP port 7412 by default. Administrators may override the port in the `[server]` section.

## Desktop client

The desktop client supports Windows and macOS.
