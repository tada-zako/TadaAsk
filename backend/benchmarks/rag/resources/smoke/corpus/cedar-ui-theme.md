# Cedar UI Theme Tokens

## Accent color

Set the accent color with the CSS custom property --cedar-accent. The value accepts any valid CSS color.

The background token is named `--cedar-surface` and does not affect the accent color.
